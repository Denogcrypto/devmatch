# Example environment variables for DevMatch

# Use Supabase or PostgreSQL URL for production / staging
# Replace <YOUR_PASSWORD> with your actual Supabase DB password.
DATABASE_URL=postgresql+asyncpg://postgres:<YOUR_PASSWORD>@db.snardferfmneudmclypf.supabase.co:5432/postgres

# Fallback local SQLite for development
# DATABASE_URL=sqlite+aiosqlite:///./devmatch.db

SECRET_KEY=replace-with-a-strong-secret
